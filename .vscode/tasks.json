{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "main",
            "type": "shell",
            "command": "cd build && cmake . .. && make && cp main .. && cd .. && ./main 2 4 0 20 0",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}